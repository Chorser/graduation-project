<image class='addBtn' bindtap='toAddNotice' src='../../images/add.png'></image>
<text>我的发布与招募</text>
<view class='page'>
  <scroll-view lower-threshold="800" bindscrolltolower="pullUpLoad" upper-threshold="0" scroll-y="true" style="height: {{windowHeight}}px;">
    <block wx:if="{{noticeList.length > 0}}">
      <navigator class='notice weui-cells' wx:for="{{noticeList}}" wx:key="noticeItem" url="/pages/noticeDetail/noticeDetail?objectId={{item.objectId}}">
        <view class='title'> {{item.attributes.title}} </view>
        <view class='content'> {{item.attributes.description}} </view>
        <view class='time'> {{item.createdAt}} </view>
      </navigator>
    </block>

    <block wx:else>
      <view class="weui-media-box weui-media-box_text">
        <view class="weui-media-box__title weui-media-box__title_in-text">
          暂无内容，请点击右下角添加</view>
        <view class="weui-media-box__desc"></view>

      </view>
    </block>
  </scroll-view>
</view>