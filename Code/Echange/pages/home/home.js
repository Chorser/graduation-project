var Bmob = require('../../utils/bmob.js');
const app = getApp()

Page({
  data: {
  },

  onShow: function () {
    // var currentUser = Bmob.User.current();//当前用户
    // console.log(currentUser)
  },

})